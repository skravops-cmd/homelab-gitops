apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: dish-lab-mongo-data
  namespace: data
spec:
  storageClassName: local-path
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
